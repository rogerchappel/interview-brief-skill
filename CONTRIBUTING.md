# Contributing

Thanks for helping improve `interview-brief-skill`. Keep changes local-first, deterministic, and fixture-backed.

## Development

```bash
npm test
npm run check
npm run build
npm run smoke
```

Add or update fixtures for parser behavior changes. Do not add live account writes or remote enrichment without a clear opt-in design and tests.
